-- 이메일 인증 여부.
--
-- 소셜(카카오) 가입자는 카카오가 이미 이메일을 확인해 준 것으로 보고, 기존 회원(이 컬럼이
-- 생기기 전부터 쓰던 계정)도 새삼 막을 이유가 없어 기본값을 true 로 둔다. 새로 가입하는
-- 이메일 계정만 애플리케이션 코드가 명시적으로 false 로 만들고, 가입 직후 인증 코드 메일을
-- 받는다. 미인증이어도 기능은 전부 그대로 쓸 수 있다 — 안내 배너만 뜬다(강제 차단 없음).
ALTER TABLE users ADD COLUMN email_verified BOOLEAN NOT NULL DEFAULT true;
