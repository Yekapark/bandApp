/**
 * 일정 도메인 — 밴드가 "이미 외부에서 잡은 합주 예약"을 앱에 기록·승인·수정·취소하고 캘린더로 조회한다.
 *
 * <p>이 서비스는 합주실 예약을 대행하지 않는다. 그래서 <b>시간대 겹침을 차단하지 않는다</b> —
 * 겹치는 일정도 그대로 저장하고, 겹침은 등록/수정 응답의 {@code overlaps}에 경고로만 싣는다
 * (BUILD_PLAN 2장 2번).
 *
 * <p>Phase 4 범위: 등록(권한 모드별 초기 status 분기)·밴드장 승인/거절·수정·취소·기간 조회,
 * 그리고 등록/취소/거절에 따른 합주실 {@code usageCount} 증감. 정기 일정(RecurringRule)·참석 체크·정산은
 * 이후 Phase 다. 다른 도메인은 이 패키지의 서비스 레이어를 통해서만 참조한다.
 */
package com.yeka.bandapp.reservation;
