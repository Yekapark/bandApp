# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
local.properties

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
bin/

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.prod
secrets/

# DB 덤프 (deploy/backup/pg-backup.sh)
backups/

# deploy/nginx/test-realip.sh 작업 디렉터리
.realip-test-*/
*firebase-adminsdk*.json

# Logs
*.log

# JVM crash artifacts
hs_err_pid*.log
replay_pid*.log

*firebase-adminsdk*.json
# 서버 .env 비교용으로 내려받은 사본 — 절대 커밋하지 않는다
env.prod.server
.env.prod.*

# 이 저장소와 무관하게 클론해 둔 외부 플러그인. 공개 저장소라 실수로 담기지 않게 막는다.
# (필요 없어지면 폴더째 지워도 된다 — 이 리포의 코드가 아니다.)
i-have-adhd/
