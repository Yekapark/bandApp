# Flutter / Dart
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
build/
.pub-cache/
.pub/
*.iml
.idea/

# 플랫폼 폴더는 `flutter create .` 로 생성 — 커밋 대상 여부는 팀에서 결정
# (초기에는 추적하지 않는다)
/android/
/ios/
/web/
/linux/
/macos/
/windows/

# 로컬 설정
lib/core/config/app_config.local.dart

# 로컬 키 (--dart-define-from-file)
dart_defines.json
